# Frontend environment example (build-time for Vite)

# Leave empty for local `npm run dev` (Vite proxies /api → :8000)
# and for Docker nginx (same-origin /api).

# For Vercel/Netlify pointing at a separate API host:
# VITE_API_BASE_URL=https://your-api.onrender.com
